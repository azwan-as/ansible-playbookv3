---
- name: Testing Handlers
  hosts: all
  tasks:
  - name: Install Dotnet4.6.1
    win_chocolatey:
      name: dotnet4.6.1
      state: present
    ignore_errors: true
    notify:
    - reboot server
  - name: failing the tasks
    fail:
      msg: Failing the task as required
    ignore_errors: true
  handlers:
  - name: reboot server
    win_reboot: